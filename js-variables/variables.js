// Оголосіть дві змінні adminName та userName. Присвойте цим змінним довільні значення. 
let adminName = 'Admin';
let userName = 'User';

// Виведіть тип введених значень з допомогою оператора typeof. 
console.log(typeof adminName, typeof userName);

// Оголосіть та присвойте змінній значення "awesome".
let yourVariableName  = 'awesome';

// Потім оголосіть ще 2 змінних та присвойте їм довільні значення. 
let firstVariable = 'not very fun but';
let secondVariable = 'cool';

// Виведіть ваш текст у форматі ( `Javascript is: ${"your variables"}` )
console.log(`Javascript is: ${firstVariable + ' ' + secondVariable}`);

// Оголосіть змінну. Присвойте їй значення. Переприсвойте значення на виведіть у консоль.
let changedVariable = 1;
changedVariable = 2;

console.log(changedVariable);